<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kdhr.mapper.DishMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    insert into  dish (name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
    values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser},
            #{updateUser})
    </insert>

    <select id="pageQuery" resultType="com.kdhr.vo.DishVO">
        select d.*,c.name category_name from dish d left outer join category c on d.category_id = c.id
        <where>
            <if test="name!=null and name !=''">
                name like "%"#{name}"%"
            </if>
            <if test="categoryId!=null and categoryId !=''">
                category_id = #{categoryId}
            </if>
            <if test="status!=null and status !=''">
                status = #{status}
            </if>
        </where>
        order by d.create_time desc
    </select>

    <delete id="deleteBatch">
        delete from dish where id in
        <foreach collection="ids" item="dishId" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </delete>
</mapper>
